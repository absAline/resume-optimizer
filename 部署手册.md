# 部署手册：AI辅助简历优化器

## 方式一：Vercel（推荐）

1. 打开 https://vercel.com/new
2. Import 仓库 `absAline/resume-optimizer`
3. 框架自动识别为 Vite
4. Build command: `npm run build`
5. Output directory: `dist`
6. Deploy

## 方式二：Netlify

1. 打开 https://app.netlify.com/start
2. 连接 GitHub 仓库
3. Build command: `npm run build`
4. Publish directory: `dist`
5. Deploy

## 本地 Ollama（免费·无需 API Key）

本项目支持用户自带 API Key（Claude/GPT），也支持连接本地 Ollama 完全免费使用。

### 安装 Ollama

参考同目录下的 `Ollama安装与模型选择指南.md`，或直接：

```bash
# macOS
brew install ollama && ollama serve

# 新终端下载模型
ollama pull qwen2.5:7b
```

### 使用

1. 确保 `ollama serve` 运行中
2. 打开页面 → 右上角 ⚙️ → 选择「Ollama (本地)」
3. 输入模型名（如 `qwen2.5:7b`），保存即用

## 本地运行

```bash
cd resume-optimizer
npm install
npm run dev       # 开发模式 http://localhost:5173
npm run build     # 构建到 dist/
npm run preview   # 预览构建结果
```

## 后台编辑器

```bash
cd admin
npm install
node server.js   # 端口 4330
```
