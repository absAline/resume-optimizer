# 部署手册：AI辅助简历优化器

## 方式一：Vercel（推荐）

1. 打开 https://vercel.com/new
2. Import 仓库 `absAline/resume-optimizer`
3. 框架自动识别为 Vite
4. Build command: `npm run build`
5. Output directory: `dist`
6. 环境变量（必填）：
   - `VITE_AI_API_KEY` — Claude API Key 或 OpenAI API Key
   - `VITE_AI_ENDPOINT` — API 端点（默认 `https://api.anthropic.com/v1/messages`）
7. Deploy

## 方式二：Netlify

1. 打开 https://app.netlify.com/start
2. 连接 GitHub 仓库
3. Build command: `npm run build`
4. Publish directory: `dist`
5. 环境变量同上
6. Deploy

## 本地运行

```bash
cd resume-optimizer
npm install
npm run dev       # 开发模式 http://localhost:5173
npm run build     # 构建到 dist/
npm run preview   # 预览构建结果
```

## 本地 API 配置

创建 `.env` 文件：

```
VITE_AI_API_KEY=你的API密钥
VITE_AI_ENDPOINT=https://api.anthropic.com/v1/messages
```

## 后台编辑器

```bash
cd admin
npm install
node server.js   # 端口 4330
```
